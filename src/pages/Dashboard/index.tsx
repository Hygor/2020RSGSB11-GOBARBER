import React, { FC } from 'react';

import { Container } from './styles';

const Dashboard: FC = () => {
  return (
    <Container>
      <h1>Dashboard</h1>
    </Container>
  );
};

export default Dashboard;
